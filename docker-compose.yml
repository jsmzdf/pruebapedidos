services:
  restaurante-app:
    build: .
    container_name: sistema-pedidos-restaurante
    ports:
      - "3021:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - restaurante-network

networks:
  restaurante-network:
    driver: bridge
